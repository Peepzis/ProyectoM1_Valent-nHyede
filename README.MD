----PROYECTO----
Nombre Git: Peepzis/ProyectoM1_Valent-nHyede
1er commit: subido para cargar el programa
-En este comit se armo los documentos html,css y js
-Incorporo variables globales
-Agrego un listener para empezar a tener control de cuando se clickea el boton y cual es el valor del select elegido
-Incorpore funciones: generarHSL(Utilizo mat floor y mat random para tener los valores aleatorios), generarCajas (genera las cajas con los colores y muestra el codigo HEX)
-Utilizo consol.log para debug y verificar que todo funcione segun lo esperado

2do commit: Se agrego la funcion hex
-En este segundo commit agregue la funcion hslToHex para convertir los valores de la funcion generarHSL a HEX
-Se consulto con la ia (chatGPT) como realizar el proceso, previamente busque por internet como pasar de color hsl a hex y encontre codigo en python
-Luego de pasar el codigo a Java y probarlo se acomodo unos valores en los numeros Hex para que den correctamente